---
  # This playbook runs a basic DF command.
  
   - hosts: all
        #remote_user: root
  
     tasks:
      - name: install yum package
        yum:
          name: httpd
          state: latest
  