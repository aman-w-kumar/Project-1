---
  # This playbook runs
    #hosts: localhost
    connection: network_cli
    gather_facts: false
    hosts: all
    #remote_user: vmadmin
  
    tasks:
     - package: name=httpd state=latest
       become: yes
  
  